declare module 'googleapis' {
    namespace googleapis {
        export interface googleapis {
            calendar(version: string): void;
        }
    }
    var obj: googleapis.googleapis;
    export = obj;
}
